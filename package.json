{
  "name": "pairwise",
  "version": "0.0.1",
  "description": "Telegram bot generating round-robin tournament matchings",
  "license": "MIT",
  "scripts": {
    "run": "ts-node src/index.ts",
    "dev": "concurrently 'tsc --watch' 'nodemon dist/index.js'"
  },
  "devDependencies": {
    "concurrently": "^8.2.1",
    "nodemon": "^3.0.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "telegraf": "^4.13.1"
  }
}
